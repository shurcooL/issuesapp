<html>
	<head>
		<link href="/assets/style.css" rel="stylesheet" type="text/css" />
		<script src="/assets/script/script.js" type="text/javascript"></script>
		<link href="//cdnjs.cloudflare.com/ajax/libs/octicons/2.1.2/octicons.css" media="all" rel="stylesheet" type="text/css" />
	</head>
	<body>
		<h1>{{.Issue.Title}} <span class="gray">#{{.Issue.Number}}</span></h1>
		<div style="margin-bottom: 20px;">{{template "issue-badge" (printf .Issue.State)}}</div>
		{{template "comment" .Issue}}
		{{range .Comments}}
		{{template "comment" .}}
		{{end}}
		{{template "post-comment" .}}
		<!--pre>{{dump .Issue}}</pre>
		<pre>{{dump .Comments}}</pre-->
	</body>
</html>
